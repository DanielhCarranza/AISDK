# LocalLLM Provider

The LocalLLM provider is a wrapper around the FoundationModels, MLX, and Local LLMs implementations. 
It provides a unified interface for interacting with LocalLLM models that can run on the device.



